# PhotoDeck Releases

Public binary releases for PhotoDeck.

Install with Homebrew:

```bash
brew tap JamesYuuu/tap
brew install --cask photodeck
```

Upgrade with Homebrew:

```bash
brew update
brew upgrade --cask photodeck
```
